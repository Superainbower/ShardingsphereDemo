<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zkml.api.warning.dao.AppealDao" >

    <select id="getAppealInfo" parameterType="String" resultType="java.util.Map">
        select appeal_content,appeal_realname,appeal_phone,DATE_FORMAT(date_created,'%Y-%m-%d %H:%i:%s') as date_created from vbox_waringinfo where handle_id = #{id} order by date_created desc limit 1
    </select>

</mapper>